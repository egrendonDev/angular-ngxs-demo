import { UserState } from './user-state';
import { CourseState } from './course.state';
import { CounterState } from './counter.state';

export const NGX_STATES = [UserState, CourseState, CounterState];
